@model bookingadmintest.Models.Booking

@{
    ViewData["Title"] = "Radera";
}

<h1 class="text-center">Avboka</h1>

<h3 class="h4 text-center">Är du säker på att du vill ta bort denna bokning?</h3>
<div class="card p-3 mb-3 mt-5 text-center" style="max-width: 800px;">
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.BookingDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Workout)
                </th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>
                    @Html.DisplayFor(model => model.BookingDate)
                </td>
                <td>
                    @Html.DisplayFor(model => model.Workout.WorkoutName)
                </td>
            </tr>
        </tbody>
    </table>
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Avboka" class="btn btn-danger" /> |
        <a class="btn btn-secondary" asp-action="Index">Tillbaka</a>
    </form>
</div>
